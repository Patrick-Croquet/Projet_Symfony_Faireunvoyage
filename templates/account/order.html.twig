{% extends 'base.html.twig' %}

{% block title 'Mes commandes | ' ~ parent() %}

{% block carousel %}<header></header>{% endblock %}

{% block body %}


<h1 class="text-center">{{ title }}</h1>

        <a href="{{ path('account') }}">Retour sur mon compte</a>

        <p>{{ paragraphe }}</p>

        {% if orders|length == 0 %}
        <hr>
        <p class="text-center">{{ paragraphe1 }}</p>
        {% else %}

        <table class="table table-hover">
            <thead>
                <tr>
                    <th scope="col">Référence</th>
                    <th scope="col">Statut</th>
                    <th scope="col">Passée le</th>
                    <th scope="col">Produit</th>
                    <th scope="col">Total</th>
                    <th scope="col"></th>
                </tr>
            </thead>
            <tbody>
                {% for order in orders %}
                <tr>
                    <td><span class="badge badge-success">{{ order.reference }}</span></td>
                    <td>
                        {% if order.state == 1 %}
                        Paiment accepté
                        {% elseif order.state == 2 %}
                        Préparation en cours
                        {% elseif order.state == 3 %}
                        Livraison en cours
                        {% endif %}
                    </td>
                    <td>{{ order.createdAt|date('d/m/Y') }}</td>
                    <td class="produit">{{ order.orderDetails|length }}</td>
                    <td>{{ ((order.carrierPrice + order.getTotal) / 100)|number_format(2, ',', '.') }}€</td>
                    <td class="text-right"><a href="{{ path('account_order_show', { 'reference' : order.reference }) }}">Voir ma commande</a></td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        {% endif %}
<style>
    @media screen and (max-width: 424px) {

        .container {
            max-width: 420px;
            margin-right: 1px;
            padding-left: 1px;
            padding-right: 1px;
        }

        h1 {
            
            margin-top: 300px;
            margin-bottom: 50px;
            font-size: 24px;
        }

        p {
            margin-top: 10px;
            font-size: 13px;
        }

        a {
            font-size: 13px;
            color: #0b93e1;
        }

        .table th {
            font-size: 14px;
            padding: 2px;
            padding-top: 10px;
            padding-bottom: 10px;
        }

        .table td {
            font-size: 12px;
            padding: 1px;
            padding-top: 10px;
            padding-bottom: 10px;
            vertical-align: middle !important;
        }

        .produit {
            width: 20px !important;
            text-align: center;
            padding-left: 0px!important;
            padding-right: 0px!important;
        }

        .text-right {
            text-align: center!important;
        }

        a:hover {
            color: #569A24;
        }

        .badge-success {
            background-color: #569A24;
        }

        .badge {
            font-size: 10px;
            white-space: normal;
        }

        #chevron {
            margin-top: 38px;
        }
    }

    @media screen and (min-width: 425px) and (max-width: 767px) {

        h1 {
            
            margin-top: 300px;
            margin-bottom: 50px;
            font-size: 24px;
        }

        p {
            margin-top: 10px;
            font-size: 13px;
        }

        a {
            font-size: 13px;
            color: #0b93e1;
        }

        .table th {
            font-size: 14px;
            padding: 2px;
            padding-top: 10px;
            padding-bottom: 10px;
        }

        .table td {
            font-size: 12px;
            padding: 1px;
            padding-top: 10px;
            padding-bottom: 10px;
            vertical-align: middle !important;
        }

        .produit {
            width: 20px !important;
            text-align: center;
            padding-left: 0px!important;
            padding-right: 0px!important;
        }

        .text-right {
            text-align: center!important;
        }

        a:hover {
            color: #569A24;
        }

        .badge-success {
            background-color: #569A24;
        }

        .badge {
            font-size: 10px;
            white-space: normal;
        }

        #chevron {
            margin-top: 38px;
        }
    }

    @media screen and (min-width: 768px) and (max-width: 1023px) {
        
        h1 {
            
            margin-top: 400px;
            margin-bottom: 50px;
            font-size: 24px;
        }

        p {
            margin-top: 10px;
            font-size: 13px;
        }

        a {
            font-size: 13px;
            color: #0b93e1;
        }

        .table th {
            font-size: 14px;
           
            padding-top: 10px;
            padding-bottom: 10px;
        }

        .table td {
            font-size: 12px;
           
            padding-top: 10px;
            padding-bottom: 10px;
            vertical-align: middle !important;
        }

        .produit {
            width: 20px !important;
            text-align: center;
            padding-left: 0px!important;
            padding-right: 0px!important;
        }

        .text-right {
            text-align: center!important;
        }

        a:hover {
            color: #569A24;
        }

        .badge-success {
            background-color: #569A24;
        }

        .badge {
            font-size: 12px;
        }

        #chevron {
            margin-top: 60px;
        }
    }

    @media screen and (min-width: 1024px) {

        h1 {
            margin-top: 450px;
            margin-bottom: 50px;
            font-size: 30px;
        }

        p {
            margin-top: 10px;
            font-size: 16px;
        }

        a {
            font-size: 15px;
            color: #0b93e1;
        }

        .table th {
            font-size: 14px; 
            padding-top: 10px;
            padding-bottom: 10px;
        }

        .table td {
            font-size: 12px;
            padding-top: 10px;
            padding-bottom: 10px;
            vertical-align: middle !important;
        }

        .produit {
            width: 20px !important;
            text-align: center;
            padding-left: 0px!important;
            padding-right: 0px!important;
        }

        .text-right {
            text-align: center!important;
        }

        a:hover {
            color: #569A24;
        }

        .badge-success {
            background-color: #569A24;
        }

        .badge {
            font-size: 12px;
        }

        #chevron {
            margin-top: 38px;
        }
    }
</style>
{% endblock %}